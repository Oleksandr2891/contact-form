import { PaymentContainerStyled } from "./paymentContainerStyled";

const PaymentContainer = ({ children }) => {
  return <PaymentContainerStyled>{children}</PaymentContainerStyled>;
};

export default PaymentContainer;
